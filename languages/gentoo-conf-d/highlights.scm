; inherits: bash
;
; Gentoo /etc/conf.d/ files — bash plus Gentoo bug references in comments.
; Mirrors gentoo-syntax/syntax/gentoo-conf-d.vim.

[
  (expansion)
  (simple_expansion)
] @variable.special
